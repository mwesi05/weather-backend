# Atmos Weather - Backend

The API server for the Atmos Weather app. It uses the free Open-Meteo geocoding and forecast APIs. No API key is required.

## Run it

1. Start the API:
   ```powershell
   node server.js
   ```

The backend runs at `http://localhost:4000`.
